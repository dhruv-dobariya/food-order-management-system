{
  "name": "food-order-management-system",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend start",
    "backend": "npm --prefix backend run server",
    "frontend": "npm --prefix frontend run dev",
    "build": "cd frontend && npm install && npm run build && cpx -r dist/* ../dist"
  },
  "devDependencies": {
    "cpx": "^1.5.0"
  }
}
